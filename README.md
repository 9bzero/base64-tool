# base64-tool

Base64 encode and decode. Also handles URL-safe Base64 (+ → -, / → _, no padding).

## Features

- Text → Base64 and Base64 → text
- URL-safe variant toggle
- File → Base64 (drag and drop)
- Copy to clipboard

## Run

```bash
npm install && npm run dev
```
